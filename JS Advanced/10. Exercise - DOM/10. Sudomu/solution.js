function solve() {

    let buttons = document.querySelectorAll("#exercise, button");
    buttons[0].addEventListener('click', (e) => {

    let cells = Array.from(document.querySelectorAll("tbody input")).map(e => e.value);
        
    });
    
}
